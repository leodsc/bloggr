import { ExtractHtmlTextPipe } from './extract-html-text.pipe';

describe('ExtractHtmlTextPipe', () => {
  it('create an instance', () => {
    const pipe = new ExtractHtmlTextPipe();
    expect(pipe).toBeTruthy();
  });
});
