import { MinimizePostTextPipe } from './minimize-post-text.pipe';

describe('MinimizePostTextPipe', () => {
  it('create an instance', () => {
    const pipe = new MinimizePostTextPipe();
    expect(pipe).toBeTruthy();
  });
});
